httpx==0.28.1
openai==1.93.0
pillow==12.3.0
# 7.0.2 gets a 403 from the synthesis endpoint: the Sec-MS-GEC token scheme
# moves server-side, so an old pin stops working. Bump, do not downgrade.
edge-tts==7.2.8

# The dashboard only (app/dashboard.py). app.main never imports these; they are
# in the shared image because the dashboard runs from it. See docs/adr/0007.
fastapi==0.115.6
uvicorn==0.34.0
jinja2==3.1.5
# Starlette parses <form> bodies through this and nothing else; without it the
# schedule POST raises at request.form(). See docs/adr/0009.
python-multipart==0.0.20

pytest==8.4.1
