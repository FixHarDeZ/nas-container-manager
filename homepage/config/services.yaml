# ══════════════════════════════════════════════════════════════════════════════
# Fix's NAS Dashboard — services.yaml
# Synology DS925+
# Secrets are injected via HOMEPAGE_VAR_* environment variables (see .env)
# ══════════════════════════════════════════════════════════════════════════════
# ─── 📥 Downloads & Monitoring ────────────────────────────────────────────────
- 📥 Downloads & Monitoring:
    - TorrentWatch:
        icon: mdi-rss
        href: "{{HOMEPAGE_VAR_DDNS_BASE_HTTPS}}:15059"
        description: Torrent Monitor (bearbit.org)
        ping: "{{HOMEPAGE_VAR_TORRENTWATCH_URL}}"
        widget:
          type: customapi
          url: "{{HOMEPAGE_VAR_TORRENTWATCH_URL}}/api/status"
          refreshInterval: 60000
          mappings:
            - field: scrape_status
              label: Status
              format: text
            - field: last_scrape
              label: Scraped
              format: text
            - field: next_scrape
              label: Next
              format: text
              
    - Download Station:
        icon: synology-download-station.png
        href: "{{HOMEPAGE_VAR_QUICKCONNECT_URL}}/download/"
        description: Synology Download Station
        widget:
          type: downloadstation
          url: "{{HOMEPAGE_VAR_NAS_URL}}"
          username: "{{HOMEPAGE_VAR_NAS_USERNAME}}"
          password: "{{HOMEPAGE_VAR_NAS_PASSWORD}}"
    
    - ink-reader:
        icon: mdi-book-open-page-variant
        href: "{{HOMEPAGE_VAR_DDNS_BASE_HTTPS}}:15068"
        description: Library for E-Reader (OPDS)
        ping: "{{HOMEPAGE_VAR_INK_READER_URL}}"
        widget:
          type: customapi
          url: "{{HOMEPAGE_VAR_INK_READER_URL}}/api/status"
          refreshInterval: 60000
          mappings:
            - field: stats.new.count
              label: New
              format: number
            - field: stats.long.count
              label: Long
              format: number
            - field: last_scrape.run_at
              label: Last Scrape
              format: string

    - dupe-sweeper:
        icon: mdi-content-duplicate
        href: "{{HOMEPAGE_VAR_DDNS_BASE_HTTPS}}:15069"
        description: หา+ลบวิดีโอซ้ำตามรหัส

    - ops-bot:
        icon: mdi-robot-love
        href: "{{HOMEPAGE_VAR_DDNS_BASE_HTTPS}}:15070/dashboard"
        description: AI Incident Response Bot

    - shorts-factory:
        icon: mdi-movie-open-outline
        href: "{{HOMEPAGE_VAR_DDNS_BASE_HTTPS}}:15071"
        description: YouTube Shorts Dashboard

    - Portainer:
        icon: portainer.png
        href: "{{HOMEPAGE_VAR_DDNS_BASE_HTTPS}}:19000"
        description: Container Manager
        widget:
          type: portainer
          url: "{{HOMEPAGE_VAR_PORTAINER_URL}}"
          env: 3
          key: "{{HOMEPAGE_VAR_PORTAINER_KEY}}"

    - Uptime Kuma:
        icon: uptime-kuma.png
        href: "{{HOMEPAGE_VAR_DDNS_BASE_HTTPS}}:13001/dashboard"
        description: Service Health Monitor
        widget:
          type: uptimekuma
          url: "{{HOMEPAGE_VAR_UPTIME_KUMA_URL}}"
          slug: "{{HOMEPAGE_VAR_UPTIME_KUMA_SLUG}}"

    - Watchtower:
        icon: watchtower.png
        href: "#"
        description: Auto Container Updates
        widget:
          type: docker
          server: my-docker
          container: watchtower

# ─── 📝 Tools & Notes ─────────────────────────────────────────────────────────
- 📝 Tools & Notes:
    - News Feed:
        icon: mdi-newspaper-variant-outline
        href: "{{HOMEPAGE_VAR_NEWS_FEED_HTTPS}}"
        description: AI & IT News Digest (Thai)
        ping: "{{HOMEPAGE_VAR_NEWS_FEED_HTTPS}}"

    - Hermes Agent:
        icon: mdi-robot
        href: "{{HOMEPAGE_VAR_HERMES_HTTPS}}"
        description: Autonomous AI Agent (Telegram + Discord)
        ping: "{{HOMEPAGE_VAR_HERMES_HTTP}}"

    - n8n:
        icon: n8n.png
        href: "{{HOMEPAGE_VAR_N8N_HTTPS}}"
        description: Workflow Automation (shared)
        ping: "{{HOMEPAGE_VAR_N8N_HTTP}}"

    - Maid Tracker:
        icon: mdi-broom
        href: "{{HOMEPAGE_VAR_MAID_TRACKER_URL}}"
        description: ระบบบันทึกการทำงานแม่บ้าน
        ping: "{{HOMEPAGE_VAR_MAID_TRACKER_URL}}"

    - Friendly Reminder:
        icon: mdi-calendar-clock
        href: "{{HOMEPAGE_VAR_DDNS_BASE_HTTPS}}:15066"
        description: ระบบติดตามการผ่อนชำระรายเดือน

    - Synology Note Station:
        icon: synology-note-station.png
        href: "{{HOMEPAGE_VAR_QUICKCONNECT_URL}}/note/"
        description: Notes & To-Do Lists

    - Synology Photos:
        icon: synology-photos.png
        href: "{{HOMEPAGE_VAR_QUICKCONNECT_URL}}/photo/"
        description: Photo Library & Sharing

    - Synology Drive:
        icon: synology-drive.png
        href: "{{HOMEPAGE_VAR_QUICKCONNECT_URL}}/drive/"
        description: Cloud File Storage

# ─── 🌐 Remote Access ─────────────────────────────────────────────────────────
- 🌐 Remote Access:
    - Jellyfin (DDNS):
        icon: jellyfin.png
        href: "{{HOMEPAGE_VAR_DDNS_BASE_HTTPS}}:18096"
        description: External Media Streaming
        ping: "{{HOMEPAGE_VAR_DDNS_BASE_HTTP}}:8096"
        widget:
          type: jellyfin
          url: "{{HOMEPAGE_VAR_JELLYFIN_URL}}"
          key: "{{HOMEPAGE_VAR_JELLYFIN_KEY}}"
          enableNowPlaying: false
          enableBlocks: true
          fields: ["movies", "series", "episodes"]

    - Plex (DDNS):
        icon: plex.png
        href: "{{HOMEPAGE_VAR_DDNS_BASE_HTTPS}}:33400"
        description: External Plex Server
        ping: "{{HOMEPAGE_VAR_DDNS_BASE_HTTPS}}:33400"
        widget:
          type: plex
          url: "{{HOMEPAGE_VAR_PLEX_URL}}"
          key: "{{HOMEPAGE_VAR_PLEX_KEY}}"
          fields: ["streams", "albums", "movies"]

    - Synology DSM (Remote):
        icon: synology-dsm.png
        href: "{{HOMEPAGE_VAR_QUICKCONNECT_URL}}"
        description: Remote NAS Management
        widget:
          type: diskstation
          url: "{{HOMEPAGE_VAR_NAS_URL}}"
          username: "{{HOMEPAGE_VAR_NAS_USERNAME}}"
          password: "{{HOMEPAGE_VAR_NAS_PASSWORD}}"
          fields: ["uptime", "volumeAvailable", "resources.cpu", "resources.mem"]

# ─── 🏠 Local Network ─────────────────────────────────────────────────────────
- 🏠 Local Network:
    - Jellyfin (LAN):
        icon: jellyfin.png
        href: "{{HOMEPAGE_VAR_JELLYFIN_URL}}"
        description: High-speed Local Stream
        widget:
          type: jellyfin
          url: "{{HOMEPAGE_VAR_JELLYFIN_URL}}"
          key: "{{HOMEPAGE_VAR_JELLYFIN_KEY}}"
          enableNowPlaying: false
          enableBlocks: true
          fields: ["movies", "series", "episodes"]

    - Plex (LAN):
        icon: plex.png
        href: "{{HOMEPAGE_VAR_PLEX_URL}}/web"
        description: Local Plex Server
        widget:
          type: plex
          url: "{{HOMEPAGE_VAR_PLEX_URL}}"
          key: "{{HOMEPAGE_VAR_PLEX_KEY}}"
          fields: ["streams", "albums", "movies"]

    - Synology NAS (LAN):
        icon: synology-dsm.png
        href: "{{HOMEPAGE_VAR_NAS_URL}}"
        description: Local NAS Management
        widget:
          type: diskstation
          url: "{{HOMEPAGE_VAR_NAS_URL}}"
          username: "{{HOMEPAGE_VAR_NAS_USERNAME}}"
          password: "{{HOMEPAGE_VAR_NAS_PASSWORD}}"
          fields: ["uptime", "volumeAvailable", "resources.cpu", "resources.mem"]




