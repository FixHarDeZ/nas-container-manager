# CLAUDE.md

Guidance for Claude Code (claude.ai/code) on project rules, architecture, and deployment for this Synology NAS Docker repository.

## 📌 Memory & Documentation Rules (Strict)

*   **Before Task:** อ่านไฟล์ใน `.notes/` ของ sub-directory นั้นๆ ก่อนเริ่มงานเสมอ
*   **After Task:** สรุปสิ่งที่ทำลงใน `<stack>/.notes/daily_log.md` ของ sub-directory ที่ทำงานอยู่เสมอ (เช่น งานใน `news-feed/` → เขียนที่ `news-feed/.notes/daily_log.md`) **ห้ามเขียนที่ root `.notes/`**
*   **Every Session End:** อัปเดตข้อมูลที่เปลี่ยนไป (DB schema, API, settings, gaps) ใน `<stack>/.notes/00_INDEX.md` ของ sub-directory นั้นๆ ควบคู่กับ log เสมอ (ไม่มีข้อยกเว้น ไม่ต้องรอ structural change)

---

## 🛠️ Environment & Deployment Gotchas

*   **NAS Environment:** Synology DSM 7.4 (Container Manager) บน DS925+ NAS Target Path: `/volume2/docker/`
*   **Secrets Vault:** Secrets ทั้ง project อยู่ใน `secrets/vault.sops.yaml` (sops+age encrypted, commit ลง git ได้). แต่ละ stack มี `secrets.manifest.yaml` ระบุว่าตัวเองใช้ key อะไร mapped จาก vault path ไหน (`env:` = secret จาก vault, `literals:` = ค่า public). Generator `scripts/render_env.py` (เรียกผ่าน `make secrets`) decrypt vault + อ่าน manifest → สร้าง `<stack>/.env` + root `.env.deploy` (gitignored)
*   **Workflow:** `make edit-vault` (sops transparently decrypt+re-encrypt) → `make secrets` → `./scripts/deploy.sh`
*   **NAS ไม่ต้องลง sops/age:** decryption เกิดที่ workstation, NAS รับ `<stack>/.env` plaintext แบบเดิมเป๊ะ — ไม่มี runtime decrypt
*   **Portability:** ไปเครื่องใหม่ → `age-keygen`, เพิ่ม public key ใน `.sops.yaml`, รัน `sops updatekeys secrets/vault.sops.yaml`, commit → เครื่องใหม่ใช้ `make secrets` ได้ (private key อยู่ที่ `~/.config/sops/age/keys.txt` เก็บใน Bitwarden/1Password ก็ได้)
*   **Deployment Flow:** ใช้ `scripts/deploy.sh` — pre-upload verify ว่าทุก stack มี `.env` ก่อน tar รวมโปรเจคต์ส่งผ่าน SSH. Root `.env.deploy` source ใน deploy.sh เพื่ออ่าน `NAS_*` (ไม่ upload ขึ้น NAS — excluded จาก tar). Restart ใช้ `docker compose --project-directory <stack>/ -f <stack>/docker-compose.yml up -d --build`
*   **⚠️ ห้ามใช้ `rsync`:** macOS bundled rsync (`openrsync` protocol 29) ไม่ compatible กับ Synology GNU rsync (protocol 31) ส่งผลให้ Transfer ล้มเหลว ให้ใช้ `tar | ssh` แทนเสมอ
*   **⚠️ SSH Multi-arg Shell Quote:** หากส่งคำสั่งที่มี pipe หรือ sub-shell ผ่าน SSH ต้องห่อเป็น single quoted string เสมอเพื่อป้องกัน remote shell ตีความพลาด: `ssh host "bash -lc \"cmd | pipe\""`
*   **⚠️ DSM Auto-Block:** Container ที่ยิง DSM API (homepage widget) ถูก auto-block IP ได้ถ้า login fail ซ้ำๆ — error code 407 ที่ homepage display เป็น "Authentication failed. 2FA enabled." จริงๆ คือ Max Tries ของ Auto Block. **Fix:** Control Panel → Security → Protection → Allow List ใส่ private subnets (`10.0.0.0/255.0.0.0`, `172.16.0.0/255.240.0.0`, `192.168.0.0/255.255.0.0`) แล้วลบ Docker IPs ออกจาก Block List

---

## 📦 Stacks & Ports Directory

| Directory | Purpose | Port (Internal / Proxy) | Critical Gotchas / Architecture |
| :--- | :--- | :--- | :--- |
| `homepage/` | Dashboard UI | 3000 / 443 | ต่อหลัง Nginx Proxy + Basic Auth. ดึง SSL จาก NAS `/usr/syno/etc/certificate/system/default/`. **Widget ห้ามใช้ HTTPS/Domain** ให้ยิงตรงไปที่ DSM HTTP `http://192.168.x.x:5000` เพื่อเลี่ยง Cert Mismatch |
| `jellyfin/` | Media Server | 8096 / — | รองรับ NVIDIA GPU Transcoding |
| `maid-tracker/` | ระบบเวลา/เงินเดือนแม่บ้าน | 5055 / — | FastAPI + SQLite (Volume `/data`). อัตรารายวัน = `เงินเดือน ÷ จำนวนวันทั้งเดือน (รวมวันหยุด — วันหยุดจ่ายด้วย)`. full month = จ่ายเต็ม, partial month/หักลา pro-rate ตาม calendar days. ลา/ชดเชยบันทึกทศนิยมได้ (`half_day`) แจ้งเตือนผ่าน LINE. **Payment schedule ต่อคน** (`payment_schedule`): `biweekly` (default, 2 รอบ 15+สิ้นเดือน) หรือ `monthly` (1 รอบ สิ้นเดือน = เต็มเงินเดือน). **Probation mode** (`employment_status`): แม่บ้านใหม่จ่ายรายวัน (`probation_daily_rate`) ลาปิด → กดผ่านโปรกลางเดือน **ยังคงจ่ายรายวันจนสิ้นเดือนที่กด**, รายเดือนเริ่มวันที่ 1 ของเดือนถัดไป (`monthly_start_date`=anchor นั้น) — `_promote_pending()` flip `probation→active` เมื่อถึง anchor (เรียกตอนกด/startup/job รายวัน 00:10 `promote_probation`). Monthly calc anchor = `monthly_start_date or start_date`. `first_month_leave_days` = `monthly_leave_days` เสมอ (transition month เป็น full month ของ anchor). **Slip โอนเงิน** (`payment_method=transfer`) + **เอกสาร id/passport/other** (`doc_label` กรณี other) หลายรูป เก็บ `/data/slips` `/data/documents` (ไม่อยู่ใน DB backup, nginx `client_max_body_size 25m` กัน 413). **ผู้จ่าย** (`paid_by` ฟิก/ปุ๊ก/ฟิก+ปุ๊ก) เลือกตอน mark paid ทั้ง daily+monthly. **Daily SQLite backup ผ่าน APScheduler 03:00** → `/data/backups/maid-*.db.gz` retention 30 วัน. **Payslip CSV** ที่ `/api/employees/{id}/payslip/{year}/{month}`. **Pay-all ค้างจ่าย**: `POST /daily-payments/pay-all` จ่ายวันค้างทั้ง window โปรทีเดียว + ปุ่ม "จ่ายทุกรอบที่ค้าง" ฝั่งรายเดือน. **กดผ่านโปรส่ง LINE ยินดี** พร้อมสิทธิที่ได้ (เงินเดือน/รอบจ่าย/วันหยุด) แนบคำแปลภาษาแม่บ้าน |
| `portainer/` | Docker Management | 9000 / — | UI สำหรับจัดการคอนเทนเนอร์ในระบบ |
| `auth/` | Centralized SSO + Password Vault | 9091 (Authelia) / 8222 (Vaultwarden) | สร้าง Docker network `auth_net` ที่ stack อื่น join. Authelia ทำ forward-auth แทน basic auth. Vaultwarden มี auth ของตัวเอง. Watchtower disabled บนทั้งสอง service |
| `uptime-kuma/`| Service Monitor | 3001 / — | ตรวจสอบสถานะการทำงานของ Services |
| `watchtower/` | Auto-update Container | — / — | มี Sidecar `watchtower-notifier` (Python 3.12-slim) อ่าน Unix Socket Log ส่ง LINE. ติดป้ายกำกับตัวเองห้ามอัปเดต: `com.centurylinklabs.watchtower.enable=false` |
| `my-secretary/`| AI Bot เลขาส่วนตัว (LINE + Telegram) | 5057 / 15057 (HTTPS)| FastAPI + Groq (`llama-3.3-70b-versatile`) + Notion Tools. มี Whitelist ID. **การเซฟลง Notion ต้องรอพิมพ์ยืนยัน "ใช่" เสมอ**. Save State ไว้ที่ `/data/state.json` |
| `hermes-agent/` | Autonomous AI Agent (Telegram + Discord) | 5063 / — | Build clones `NousResearch/hermes-agent` from GitHub (`ARG HERMES_REF`). Dashboard has built-in basic auth (vault-managed: `stacks.hermes_agent.dashboard.*`). `config.yaml` auto-generated on first run. `network_mode: host` dropped — uses bridge networking. Port 5060/5061 blocked by browsers (SIP), uses 5063. Auth injected via `scripts/inject-dashboard-auth.sh` wrapper. |
| `torrentwatch/` | Scraper & Filter | 5059 / 15059 (HTTPS)| FastAPI + Volume `/data`. สแกน bearbit.org ตามช่วงเวลา (19:00-01:00 รันทุก 30 นาที / 01:00-06:00 หยุด / 06:00-19:00 รันทุก 60 นาที) โหลดเข้า `/downloads` ตรง. ลบประวัติเก่าเกิน 7 วัน ทุกวัน 03:00 (+ ตอน startup เพื่อ enforce retention หลัง restart). มี Endpoint `/api/status` แบบ Public. **H&R monitor**: อ่าน `myhr.php` ทุก 6 ชม. (03:10/09:10/15:10/21:10) คำนวณ slack = `(เวลาโหลดเสร็จ + 168 ชม. − now) − ชม.ที่ยังต้อง seed` แจ้ง LINE+Telegram เมื่อ slack < `hr_slack_hours` (default 24) — dedup ด้วย meta `hr_last_digest`. **H&R report รายวัน**: `hr_report_enabled`/`hr_report_time` (ตั้งใน Settings) ส่งสถานะทุกไฟล์เข้า Telegram วันละครั้ง — read-only ไม่ถามลบ/ไม่ auto-fix, แยกจาก `hr_notify_enabled`, reschedule ทันทีตอน `PUT /api/settings`. **H&R auto-fix**: ไฟล์สถานะ `warn` ที่ tracker ไม่เห็น client เกิน `hr_fix_stale_hours` (= task ใน DS หายไป) → บอทถาม Telegram ด้วยปุ่ม กดยืนยันแล้วถึงโหลด .torrent ลง `/downloads` (watch folder) และแจ้ง LINE+Telegram อีกครั้งเมื่อ seed ครบพ้น H&R. **Task ที่ DS ไม่ขยับ**: มี task แต่ยังไม่ seed → `dsm.nudge_task()` (pause+resume บังคับ hash check) ทุกรอบ ไม่รอ grace — ตัวนับ progress ของ DS อัปเดตเฉพาะตอน hash check (พังทั้งเครื่องหลัง OOM outage 19/08 `size_downloaded` ค้าง 0 ทั้งที่ไฟล์ลงดิสก์จริง) task ที่ครบแล้วจึงไม่พลิกเป็น seeding เอง; เกิน 120 ชม. ยังไม่ขยับ = `wedged` แจ้ง Telegram ครั้งเดียวให้คนไปลบเอง (เคสแก้ไม่ได้คือชื่อในตัว torrent เกิน 255 ไบต์ ext4/btrfs เขียนไม่ได้). Callback รับเฉพาะ chat_id ที่ตั้งไว้, prompt หมดอายุ 12 ชม., scan/cleared-check ต้องอยู่**เหนือ** dedup `hr_last_digest` ไม่งั้นไม่รันจริง. **ลบหลังพ้น H&R** (`hr_delete_enabled` default off): ถาม Telegram **2 จังหวะ** — ปุ่มแรกแค่ไปดึง DS task + real path มาโชว์ (ยังไม่ลบ) ปุ่มที่สองถึงลบ task แล้วค่อยลบไฟล์ หรือเลือก **ลบเฉพาะ task (เก็บไฟล์)** ได้ (DS delete ใช้ `force_complete=false` ไม่แตะ payload). ทุกปุ่มที่จบเกม edit ข้อความเดิม (ปุ่มหาย) + ยิงข้อความใหม่ เพราะ editMessageText ไม่มี noti. ใช้ DSM WebAPI **ผ่าน HTTP** (`SYNO.DownloadStation.Task` v1 + `SYNO.FileStation.List/Delete`) — CLI `synowebapi` ใช้ไม่ได้ เสิร์ฟเฉพาะ core API. `FileStation.Delete` **ลบถาวร ไม่เข้า #recycle**. หา real path ไม่เจอ = เหลือปุ่มลบเฉพาะ task อย่างเดียว ห้ามเดา path. Secrets: `stacks.torrentwatch.dsm.*` (บัญชีเดียวกับ homepage widget).  ⚠️ `myhr.php` เสิร์ฟ **windows-874** แต่ httpx บอก utf-8 → ต้อง decode `resp.content` ด้วย **`cp874`** |
| `news-feed/` | AI & IT News Feed Bot + Dashboard | 5064 / — | Single container: FastAPI + APScheduler + SQLite. Summariser switchable via dashboard (Anthropic default, OpenRouter/DeepSeek option). Digest sent to LINE + Telegram at 07:00/12:00/18:00. |
| `secretary/` | Personal Knowledge Base (Notion→Qdrant RAG + n8n Automation) | 5065 (query) / 15065 · 5678 (n8n) / 15678 | Multi-service stack: qdrant, ollama, n8n, secretary-query (FastAPI), secretary-ingest (one-shot). BGE-M3 ~2GB download on first run (cached in `hf_cache` volume). Ingest subservice: `restart: "no"`, run with `docker compose run --rm secretary-ingest`. Sub-services have own `.env` at `ingest/.env` and `query/.env`. |
| `friendly-reminder/` | ระบบติดตามการผ่อนชำระรายเดือน | 5066 / — | FastAPI + SQLite (Volume `/data`) + Nginx Basic Auth. บันทึกรายการผ่อน (ชื่อ, ราคา, จำนวนงวด, เดือนเริ่มต้น) — auto-generate payment rows ตลอดอายุสัญญา. **`due_day` ต่อรายการ** (วันครบกำหนดของเดือน เช่นทุกวันที่ 25, clamp วันเกินเดือนด้วย `calendar.monthrange`). กดปุ่ม "จ่ายแล้ว" ต่องวด แจ้ง LINE เมื่อจ่าย. APScheduler ส่ง LINE reminder ทุกวันเวลา `REMINDER_TIME` (default 08:00) สำหรับงวดที่ครบกำหนด/เกินกำหนด **ยิงซ้ำทุกวันจนกว่าจ่าย** + day-before reminder 1 วันก่อน `due_day` (`DAY_BEFORE_REMINDER_TIME` default 20:00). Export CSV ผ่าน `/api/report`. **LINE webhook** `/webhook/line` (public ผ่าน DSM RP `:15066`, nginx ข้าม basic auth, app ตรวจ `X-Line-Signature`): โพสต์สลิปในกลุ่ม → แนบสลิป+mark paid อัตโนมัติ (งวดค้างเดียว=auto, หลายงวด=บอทถามชื่อ, "ค้าง"=ครบกำหนด≤เดือนนี้ & unpaid). Vault keys: `stacks.friendly_reminder.line.*` (`channel_access_token`, `channel_secret`, `group_id`). **LINE OA: Use webhook ON + Auto-reply OFF** |
| `dupe-sweeper/` | หา+ลบวิดีโอซ้ำตามรหัส (dashboard) | 5069 / — | FastAPI + SQLite + static dashboard (vanilla JS, light/dark) หลัง Nginx Basic Auth. Mount media path (`/volume1/private_media/<library>` → `/media/<library>`) **read-write** เพื่อลบได้. Scan `os.walk` (skip `@eaDir`/`#recycle`/`.dupe-sweeper-trash`) → group by media code (SSNI-618==SSNI-00618, FC2-PPV, HEYZO, caribbean date form) → dashboard เลือกเก็บ/ลบต่อไฟล์. **Delete = trust boundary**: ทุก path ต้อง `realpath` แล้วอยู่ใต้ `SCAN_ROOTS` ไม่งั้น reject ทั้ง request (`recycle.py`). ลบ = ย้ายเข้า recycle (`TRASH_DIR` บน volume เดียวกัน = `os.rename` atomic ไม่ copy), empty เอง. รันเป็น root ตั้งใจ (เลี่ยง Synology dynamic-UID trap, LAN-only). ไม่มี app secret — auth อยู่ใน nginx `.htpasswd` จาก vault `stacks.dupe_sweeper.dashboard.*` |
| `ink-reader/` | Doujin library + OPDS สำหรับ Meebook M8 | 5068 / 15068 | Scrape doujin-th.com → CBZ ใน /data/library. Lifecycle new(auto-expire)/deleted(tombstone กันโหลดซ้ำ) — kept feature ถูกถอดออกแล้ว. **Runtime settings ใน DB** (`retention_days` default 30, `min_pages` default 30) แก้ผ่าน dashboard ⚙️ / `PUT /api/settings`; เปลี่ยน retention แล้ว recompute expiry ทุกเรื่อง. แท็บ "หน้าเยอะ" + OPDS `/opds/long` filter เรื่อง pages ≥ `min_pages`. KOReader ต่อ OPDS /opds ผ่าน nginx basic auth (vault: stacks.ink_reader.dashboard.*). Fallback สร้าง CBZ จากรูปหน้าอ่านถ้าลิงก์ download พัง |
| `ops-bot/` | AI Incident Response Bot | 5070 / 15070 | FastAPI + Paramiko SSH + mimo LLM. รับ webhook จาก Uptime Kuma, SSH เข้า NAS รัน diagnostics, วิเคราะห์ root cause, แจ้ง Telegram พร้อม InlineKeyboard fix. Watchtower grace period 5 นาที. Commands: `/status`, `/diagnose`, `/logs`. Dashboard ที่ `/dashboard`. Secrets: `stacks.ops_bot.*`. Fix-as-PR: proposes config fixes as GitHub PRs (read-only on NAS, human merges+deploys). **Nginx sidecar (`ops-bot-nginx`) ถือ port 5070** — app `expose: 8000` เท่านั้น ไม่ publish. Basic auth ทุก path (`nginx/.htpasswd` จาก vault `stacks.ops_bot.dashboard.*`) **ยกเว้น** `/webhook/uptime-kuma` เพราะ Kuma ส่ง basic auth ไม่ได้ — path นั้นกันด้วย `KUMA_WEBHOOK_SECRET` (`?secret=`) ต้องไม่ว่างเสมอ ไม่งั้นใครก็ปลอม incident ได้ |
| `shorts-factory/` | Telegram bot สร้าง YouTube Shorts ไทย (DevOps/AI) | 5071 (dashboard) / 15071 | **บอทเองไม่มี HTTP** — ไม่มี FastAPI/nginx/port ในโปรเซสบอท มีแค่ Telegram `getUpdates` long-poll loop (ก๊อปแพตเทิร์นจาก `ops-bot/app/commands.py`) ตัวกันเดียวคือ filter `TELEGRAM_CHAT_ID` ทิ้ง update จากคนอื่นทั้งหมด. Flow: พิมพ์หัวข้อ → mimo คืน Script (hook + cards + title/description/hashtags) → **หยุดรอคนอ่าน** → ปุ่ม render / เขียนใหม่ (พิมพ์ feedback แล้ว mimo เขียนใหม่ตาม) → ต่อ card: ดึง stock footage จาก Pexels (ฟรี) + edge-tts (`th-TH-NiwatNeural`) + Pillow วาดการ์ด → ffmpeg composite (footage + scrim `black@0.5` + การ์ดโปร่งใส) แล้ว concat →  ส่ง mp4 เข้า Telegram + วางไฟล์ที่ `/volume1/shorts` พร้อม `.txt` metadata. ความยาว card มาจาก duration ของเสียง ไม่ใช่ค่าตายตัว. ไม่มี scheduler (ทำงานตอนมีหัวข้อเข้า) ไม่แตะ YouTube API (ดู `docs/adr/0001` — API compliance audit ล็อกคลิปเป็น private + OAuth `Testing` refresh token ตาย 7 วัน). **⚠️ ฟอนต์: ใช้ `Waree-Bold` จาก `fonts-thai-tlwg`** ห้ามใช้ NotoSansThai — cmap ไม่มีละตินเลย คำอังกฤษกลายเป็นกล่องเปล่า และ Pillow ไม่มี font fallback. **⚠️ Pillow wheel ไม่มี Raqm** ต้อง `apt install libraqm0` ไม่งั้น `ImageFont.Layout.RAQM` ถอยไป basic layout **เงียบๆ** วรรณยุกต์ไทยหาย (แค่ `UserWarning`) — Dockerfile assert `features.check('raqm')` ตอน build กันพลาด. ffmpeg `drawtext` ไม่ทำ shaping ใช้แทนไม่ได้. `mem_limit: 2g` + `cpus: 3.0` เพราะเครื่องเคย OOM ทั้งตัว 19/08. ไม่มี footage (ไม่มีคีย์/หาไม่เจอ/โหลดพัง) → การ์ดนั้น fallback เป็นพื้น gradient + Ken Burns เงียบๆ ไม่ล้ม. **เสียงพูดยิง TTS ครั้งเดียวทั้งคลิป** แล้วตัดภาพตาม `SentenceBoundary` — ขึ้นบรรทัดใหม่คั่น card คือสิ่งที่ทำให้ endpoint ยิง boundary แต่มันก็เป็น paragraph break ที่แถมความเงียบ ~1.0 วิ ทุกรอยต่อ card (วัดจริง เทียบกับ 0.12-0.53 วิ ตรงจังหวะหายใจปกติของเสียงนี้) → `render.tighten()` ตัดเสียงตาม boundary แล้วเล็มหางเงียบเหลือ `JOIN_SILENCE` 0.30 วิ ก่อนต่อกลับ พร้อมวัด start ใหม่จากไฟล์จริง (offset ที่ endpoint บอกยาวเกินจริง). **การ์ดมี narration 2 ชุด**: `narration` (คำอังกฤษคงไว้ ใช้ขึ้นซับ) กับ `spoken` (ทับศัพท์ไทยล้วน อ่านออกเสียงเฉพาะตัวนี้) — `validate()` ตีกลับถ้า `spoken` มีอักษรละติน เพราะเสียงจะสลับไปสำเนียงอังกฤษกลางประโยค พูดรัวจนฟังไม่ทัน (ไทยไม่มี `WordBoundary` เพราะไม่เว้นวรรค) — segment เป็นวิดีโอล้วน `-an` ค่อย mux เสียงทับตอนท้าย, boundary ไม่ตรงเมื่อไหร่ fallback ไปพูดทีละ card. **`/redo`**: render `state["last_script"]` ซ้ำ ไม่ยิง LLM ใส่ `clip_id`/`topic` กลับจาก `last_*` ให้ลง manifest เดิม — มีไว้เพราะแก้ `/say` แล้วเสียงเก่าไม่เปลี่ยนเอง และเขียนสคริปต์ใหม่ = คนละสคริปต์. ข้อความขึ้นต้น `/` ที่ไม่ตรงคำสั่งไหน ตอบ "ไม่รู้จักคำสั่ง" ไม่ตกไปเป็นหัวข้อ (เคยได้ manifest ชื่อ `/stat` มาแล้ว). **⚠️ ไทยไม่มีเสียง /s/ ท้ายพยางค์** — ส ษ ศ ซ เป็นตัวสะกดออกเสียง /t/ หมด `พาส` อ่าน `พาด` ตามกฎ ไม่ใช่ TTS เพี้ยน แก้ด้วยการสะกดไม่ได้ ต้อง `/say` เป็นคำอังกฤษ หรือเติมสระให้ ส เป็นพยัญชนะต้น (`พาสะ`). **หัวข้อทรงผลแข่ง/ข่าว บอทไม่รับ** (`RESULT_TOPIC` ใน main.py): โมเดลไม่รู้ผลแข่ง เขียนออกมาเป็นความรู้ทั่วไปโครงเดิมทุกครั้ง + แต่งตัวเลขเอง (วัดจริง 6 คลิปวอลเลย์ 29-31/08 title ซ้ำเป๊ะ 2 คลิป, 2 คลิปที่อัปแล้วมีตัวเลขมั่ว). ด่านอยู่ใน `make_script()` ไม่ใช่ `on_text` เพราะทางเข้าหัวข้อมี 3 ทาง (พิมพ์เอง/ปุ่ม trends/auto-pick) ลงมาที่นี่หมด — เช็คก่อน `manifest.start()` และก่อน pop `auto_pick` (ปฏิเสธแล้วต้องไม่ทิ้ง manifest เปล่า ไม่กิน variant), ไม่เช็คตอนแก้สคริปต์, ใส่ `!` นำหน้าหัวข้อ = ข้ามด่าน. **`/say <ผิด> = <ถูก>`**: แทนที่คำตอนอ่านออกเสียง เก็บที่ `/data/say.json` คีย์เป็นไทย (spoken ไม่มีละตินอยู่แล้ว) — โมเดลสะกดคำเล่นคำทีละตัวอักษร (TH-AI Passport → `ทีเอไอพาสปอร์ต`) และ edge-tts อ่านคำไทยที่สะกดถูกบางคำเพี้ยน กดเขียนใหม่ไม่ช่วยเพราะได้ข้อความเดิม. แทนที่ใน `render._tts_text()` **เท่านั้น** ถ้าไปทำตอน join ใน `narrate()` boundary check จะเทียบคนละเวอร์ชัน misalign ทุก card แล้วถอยไปพูดทีละ card เงียบๆ. สคริปต์ตอนรีวิวโชว์บรรทัด 🗣 (`spoken` ที่ต่างจาก `narration`) ไว้ก๊อปคำผิดจากมือถือ. **ซับ**: สร้าง `.srt` จากขอบเขตประโยคเดียวกับที่ใช้ตัดภาพ (ใช้ narration ต้นฉบับ ไม่ใช่ตัวทับศัพท์) แนบผ่าน `captions.insert`. **Manifest**: `/data/clips/<id>.json` 1 ไฟล์ต่อ Topic เก็บ draft ทุกรอบ (รวมที่กดทิ้ง) + render params + start ต่อ card — workdir ถูกลบทุกครั้ง อะไรไม่เก็บตรงนี้หายถาวร. **Gate** (`docs/adr/0004`): ห้ามป้อนผลตัวเองกลับเข้า prompt จนกว่าจะครบ 30 คลิป (+300 views/variant ตอนเริ่มทดลอง) — `winning_examples()` คืน `[]` ก่อนถึง Gate เพราะ 9 คลิปแรก 206 views มาจากคลิปเดียว 182. **Snapshot รายวัน**: `app/snapshots.py` ดึงตัวเลขคลิปอายุ 0-30 วันหลัง `SNAPSHOT_HOUR` (default 10) เขียนลง manifest — **ขี่ poll loop ไม่ใช้ scheduler thread** ไม่เปิดพอร์ต, `/snapshot` สั่งมือได้, `manifest.day7()` = ตัวเลขทางการ (retention ขยับตามวิวที่เข้ามา เทียบ 'ล่าสุด' = เทียบคลิปเก่ากับใหม่). `app/backfill.py` กู้ manifest คลิปเก่าจาก `.txt`/`.srt` ใน `/output` ตอน startup (idempotent, ธง `reconstructed`). **Auto /trends**: บอทสั่ง `/trends` เอง `TRENDS_HOURS` (default `8,12,17` เวลาไทย) ขี่ poll loop เหมือน snapshots — stamp slot **ก่อน** spawn (ไม่งั้นยิงซ้ำทุก 30 วิ) และเป็นหนี้แค่ชั่วโมงล่าสุดที่ผ่านมา (ดับทั้งวันแล้วกลับมา = รอบเดียว). ลิสต์รอบอัตโนมัติมีปุ่ม ✋ เพิ่ม (พก `suggested_at` เหมือนปุ่มเลข) ไม่กดอะไรใน `AUTO_PICK_MINUTES` (default 15) = สุ่มหัวข้อ 1 อัน เขียนสคริปต์แล้ว **render ให้เลย** ไม่ต้องรีวิว — สคริปต์รอบอัตโนมัติส่งแบบ**ไม่มีปุ่มและไม่เก็บ message_id** เพราะ `do_render()` เขียนทับข้อความที่มันถือ จะลบสคริปต์ที่ไม่มีใครอ่านทิ้ง. deadline ยิงเฉพาะตอน idle (คนกำลังทำคลิปอยู่ = ทิ้งรอบนั้น ไม่ต่อคิว), เริ่มหัวข้อทางไหนก็ตามยกเลิก pending. **footage จาก Google Flow** (ADR 0005): ปุ่ม 🎨 บนสคริปต์ = mimo เขียน Flow Prompt อังกฤษให้ hook card (ส่งเป็น HTML `<pre>` = Telegram มีปุ่มก๊อป) แล้ว**พักคลิป**ไว้ใน `state["parked"]` บอทกลับเป็น idle รับหัวข้อใหม่ได้ — คนเจนในแอป Flow เอง แล้ว **reply ข้อความ prompt นั้นพร้อมแนบ mp4** (`reply_to_message.message_id` คือตัวผูกไฟล์กับ card ห้ามใช้ชื่อไฟล์/ลำดับ/คีย์เวิร์ด เดาผิด = คลิปสวยแต่ผิดเรื่อง) → เก็บที่ `/output/footage/<clip_id>/c00.mp4` → `render.build(supplied={0: path})` ข้าม Pexels เฉพาะ card นั้น. พักได้ทีละ 1 คลิป หมดอายุ 24 ชม. (`FLOW_PARK_HOURS`) = `outcome=abandoned` ตรวจในลูป poll (pop ออกจาก state ก่อน spawn ไม่งั้นยิงซ้ำทุก tick), `auto_pick_due()` คืน False ตอนมีคลิปพัก. ไฟล์ >20MB โหลดไม่ได้ (`getFile` cloud API) ให้ส่งแบบวิดีโอธรรมดา. **ไม่ยิง Veo API** — Flow credits ≠ API billing และ Standard ~$0.40/วิ ≈ $18/คลิป. **storyboard** (ADR 0006): ปุ่ม 📋 บนสคริปต์ = วาง storyboard 9:16 (1 card = 1 ฉาก), `/storyboard <บรีฟ>` = คลิปยาว 16:9 ดีฟอลต์ 4 ฉาก — **ปลายทางคือ Google Flow ไม่ใช่ ChatGPT** (Flow ไม่มีเมนู storyboard ของจริงคือ Ingredients to Video แนบภาพอ้างอิง 3 ภาพ + Frames to Video + Scenebuilder, image model ในตัวคือ Nano Banana Pro). ส่ง N+2 ข้อความ (overview + ตัวละคร + ฉากละใบ) ฉากละ 2 กล่องก๊อป: image prompt / image prompt + motion. **`validate()` บังคับ**: ทุกฉากต้องมี `locked_prompt_tag` คำต่อคำ (ไม่งั้นหน้าตัวละครเปลี่ยนรายฉาก), prompt ห้ามมีอักษรไทย, ต้องระบุอัตราส่วน, ต้องมี `no text, no watermark, no UI, no split-screen`, จำนวนฉากเท่าจำนวน card. `sound_verbatim`/`on_screen_text` **เขียนทับจาก Script** (`lock_to_script()`) ไม่ตรวจ. เส้น Shorts ยิง LLM (~60 วิ) ไม่ใช่เทมเพลตล้วนแล้ว, ไม่แตะ state, อ่าน `clip_id` ก่อนเรียกโมเดล, เก็บ JSON ลง `manifest.storyboard`. **บอทไม่ประกอบคลิปยาว**. **อัป YouTube ยังต้องกดเอง** (ADR 0001). **Trends** (`app/trends.py`, `/trends`): Google Trends RSS ไทย (`/trending/rss?geo=TH` — `dailytrends` ตายแล้ว 404) + YouTube `chart=mostPopular&regionCode=TH` → mimo แปลงเป็นหัวข้อ 5 อัน ส่งของดิบมาด้วยเสมอ. **ตัดหมวด 25 (ข่าว/การเมือง) และ 17 (กีฬา) + พรอมป์ห้ามหัวข้อทรงข่าวลือเรื่องคนจริง** ไม่งั้นบอทจะแต่งเรื่องคนจริงแล้วอัปขึ้นช่อง. **หัวข้อไม่ล็อก DevOps/AI แล้ว** (`devops ไทย` ค้นบน YouTube ได้ 0 ผลลัพธ์ใน 30 วัน) — `category` เป็นฟิลด์บังคับใน Script อ่านแบบสังเกตการณ์ ไม่ใช่ variant ที่สุ่ม (คนเลือกหัวข้อเอง). **Experiment** (`app/experiment.py`): factor `hook` 2 variant สุ่มต่อคลิปตอนเปิด Topic **ก่อน**เขียนสคริปต์ ไม่ re-roll ตอนสั่งแก้ (ไม่งั้นคนเลือกผู้ชนะเอง), explore 1 ใน 3 ไม่นับผล, เก็บ clause คำต่อคำใน manifest (พรอมป์หลักจะ drift). `/experiment` ฟันธงต่อเมื่อ ครบ 10 คลิป + 300 views ทั้งสอง arm และ median day-7 ต่าง ≥5 จุด ไม่งั้นตอบ "สรุปไม่ได้". **Retention** (`app/retention.py`, `/retention`): **Shorts มีเส้น retention จริง** (ยืนยัน 27/08: 361 views ได้ 100 แถว, 27 views ได้ 0 — ประตูคือยอดวิวไม่ใช่ฟอร์แมต) ดึง `audienceWatchRatio` ต่อ `elapsedVideoTimeRatio` แล้วคูณความยาวคลิปเพื่อ map กลับเป็น card (นี่คือเหตุผลที่ manifest เก็บ card start), หา cliff = ชันกว่า step ปกติ 2 เท่า และ ≥5% ของความสูงเส้น รวม bucket ติดกันเป็นก้อนเดียว, วาด PNG ด้วย Pillow ส่ง `sendPhoto`. Reports API ตอบ 500 รายคลิปเป็นครั้งคราว → ข้ามคลิปนั้น ไม่ล้มทั้งคำสั่ง. **ประวัติ**: `/data/history.json` บันทึกทุกคลิปที่อัป → ป้อนชื่อ 30 อันล่าสุดเข้า prompt กันเขียนซ้ำ. **`/stats`**: ดึง views + % ที่ดูจนจบต่อคลิป เรียงตาม % (ไม่ใช่ยอดวิว) + ป้อน 3 อันดับแรกเข้า prompt เป็นตัวอย่าง — on-demand ไม่มี scheduler ตาม ADR 0002. **⚠️ mimo ค้างเป็นช่วงๆ (tail latency)**: บางครั้ง `200 OK` มาทันทีแล้ว body ไม่มาเลยจนครบ 600 วิ (ยืนยัน 27/08 19:25) แต่หัวข้อเดิมอีก 2 ชม.ต่อมาเสร็จใน 137 วิ → `_say()` ทำ **hedge**: ครบ 240 วิยิงคำขอที่สองคู่ไปที่ **`MIMO_FALLBACK_MODEL` (`mimo-v2.5`) ไม่ใช่ pro ตัวเดิม** เพราะยิง pro ซ้ำค้างพร้อมกันทั้งคู่. **ห้ามใช้ `max_tokens` คุมเวลา** — reasoning tokens กินโควตาก่อน ได้ `content=''` กลับมา. **⚠️ เวลาของ mimo แปรตาม token ที่คิด ~30 tokens/วิ** (วัด: 93s/3k, 112s/4k, 197s/7k, 207s/5.4k, 347s/10.6k) ไม่ได้ค้างมั่ว → เพดาน `MIMO_TIMEOUT_SECONDS` = **600 วิ แชร์ข้าม retry ทุกรอบ — ลูปขับด้วย deadline ไม่ใช่จำนวนรอบตายตัว (เพดาน 4 รอบ)** เพราะคำตอบเสียที่กลับมาเร็วเคยกิน โควตารอบทิ้งทั้งที่เหลือเวลา 420 วิ (04/09) (เดิม 240 ครั้งละครั้ง = ตัดรอบคิดยาวทิ้งแล้วเงียบ 8 นาที). **streaming ลองแล้วแย่กว่า**: คำตอบเดียวกัน stream 400 วิ vs ไม่ stream 137 วิ. **งานยาว (`make_script`/`do_render`/`do_upload`/`on_trends`) รันนอก poll loop ด้วย `spawn()`** บอทจึงตอบคำสั่งอื่นได้ระหว่างทำงาน รับทีละชิ้น. **⚠️ เรียก LLM ต้องครอบ `asyncio.wait_for` เสมอ** — `timeout` ของ httpx เป็น per-read ไม่ใช่เพดานรวม เซิร์ฟเวอร์ที่หยอดข้อมูลทีละนิดจะไม่มีวัน timeout และ poll loop รัน inline บอทจะแข็งทั้งตัว. **BGM** วางไฟล์ที่ `/volume1/shorts/bgm/` สุ่มเพลงต่อคลิป ducking ด้วย `sidechaincompress` ที่ key ด้วยเสียงพูด (โฟลเดอร์ว่าง = ไม่มีเพลง). **อัป YouTube**: ปุ่มใต้คลิปที่ส่งเข้า Telegram (ไม่อัตโนมัติ เพราะออกไปข้างนอกแล้วเอากลับไม่ได้) — `app/youtube.py` ใช้ httpx ล้วน resumable upload และ**อ่าน `privacyStatus` ที่ API ตอบกลับ** ไม่เดา (โปรเจกต์ที่ยังไม่ผ่าน API audit จะถูกบังคับเป็น private). ตั้งปกจากเฟรมแรกได้ (`YOUTUBE_SET_THUMBNAIL` default **false**) — **ฟีด/กริด Shorts ไม่ใช้ custom thumbnail** ใช้แค่หน้าค้นหา/แนะนำ. **⚠️ `MIMO_REASONING_EFFORT=low`** จำเป็น — default ของ mimo-v2.5-pro เผา 10,457 tokens/161 วิ เทียบกับ 3,796/79 วิ แถมสคริปต์แย่กว่า. ตั้งค่าครั้งเดียวด้วย `scripts/youtube_auth.py` — **consent screen ต้องเป็น `In production`** ถ้าค้าง `Testing` refresh token ตายใน 7 วัน. ไม่มี credential = ปุ่มไม่โผล่. Secrets: `stacks.shorts_factory.telegram.*`, `stacks.shorts_factory.pexels_api_key`, `stacks.shorts_factory.youtube.*` + ใช้ `shared.llm.mimo_api_key` ร่วมกับ news-feed **Dashboard read-only 5071** (`app/dashboard.py` + nginx sidecar, ADR 0007 แก้ 0002): คอนเทนเนอร์แยกจากบอท อิมเมจเดียวกัน `command` ต่างกัน mount `/data:ro` — **บอทยังไม่มีพอร์ต** และ dashboard **ไม่ใส่ `env_file`** ไม่ถือ bot token/YouTube refresh token เลย (โปรเซสเดียวที่ LAN ถึง). หน้า `/` ลิสต์คลิปทุกอัน + ตัวเลข day-7, `/clip/{id}` manifest เต็มรวม draft ที่กดทิ้ง + start ต่อ card, `/experiment` สอง arm + verdict + by_category, `/now` state.json + say.json + คลิปที่อัปล่าสุด. **ไม่มี route ที่ไม่ใช่ GET** มีเทสต์ยืน (`test_no_route_can_write`) — สั่งงานบอทยังทำใน Telegram อย่างเดียว. ไม่ import `app.render` (อ่าน `say.json` เอง) เพื่อไม่ให้ Pillow/edge-tts อยู่ในโปรเซสที่เปิดพอร์ต. auth = nginx basic auth ทุก path จาก vault `stacks.shorts_factory.dashboard.*`. **English Locale** (`app/locales.py`): Locale ผูกทุกอย่างที่เปลี่ยนตามผู้ชม (voice, ความกว้างบรรทัด, ภาษา trends, โฟลเดอร์ปลายทาง, ช่อง YouTube) ไว้เป็นก้อนเดียว คีย์ด้วยโค้ดสั้น (`th`/`en`) — โค้ดไม่รู้จักหรือไม่มี field (manifest เก่าก่อนมี Locale) ถอยไปเป็น `th` เสมอ, อ่าน env ตอนเรียกใช้ ไม่ใช่ตอน import. `/en <หัวข้อ>` เขียน+render คลิปอังกฤษ เสียง `en-US-AndrewNeural` (`TTS_VOICE_EN`, เพิ่มใน secrets.manifest.yaml เป็น literal). `/trends en` ดึง Google Trends US + YouTube US chart แทนไทย ปุ่มเลขจากลิสต์นั้นได้คลิปอังกฤษเลย ภาษาที่ไม่รู้จักหลัง `/trends` ถูกปฏิเสธ ปุ่ม/ข้อความบอททั้งหมดยังเป็นไทยเสมอ. **บรรทัดอังกฤษกว้างกว่าไทยเท่าตัวใน Waree-Bold** เพดานเลยสั้นกว่า (24 ตัวอักษร เป้า 18) และ**บังคับนับตัวอักษรจริง**ผ่าน `enforce_char_count` ใน `script._too_wide()` (พิกเซลอย่างเดียวจะปล่อยบรรทัดละตินยาว 38 ตัวผ่านแล้วไปวาดที่ font 40px ขั้นต่ำ) — ไทยยังวัดพิกเซลอย่างเดียว 34 ตัวอักษรเป็นแค่ guidance ให้โมเดล ไม่บังคับ. กฎ `spoken` สลับด้าน: ไทยห้ามละติน อังกฤษห้ามไทย, `render._speakable()` ต่อคำด้วย space สำหรับอังกฤษ ไม่มีตัวคั่นสำหรับไทย — edge-tts ยิง `SentenceBoundary` ให้ `en-US-AndrewNeural` เหมือนเสียงไทย (วัดจริง 3 card เข้า 3 boundary ออก 2026-09-07) เลยเดินไปป์ไลน์ tighten/join เดียวกันได้เลย ไม่มี path แยก. ไฟล์อังกฤษลงที่ `/volume1/shorts/en` (`/output/en` ในคอนเทนเนอร์) ไทยยังลง `/output` เหมือนเดิม, `app/backfill.py` เดินด้วย `rglob` แทน `glob` เพื่อเจอทั้งสองโฟลเดอร์ แล้วอ่าน locale จากชื่อโฟลเดอร์. **Experiment**: factor `hook` เดิม 2 variant ชื่อเดิม แต่ clause เขียนเป็นภาษาของคลิปเอง (`experiment.VARIANTS_EN` + explore clause อังกฤษ) — **นับแยกช่อง** `experiment.for_locale()` กรองก่อน `/experiment` พิมพ์ทีละ locale (ไทยเสมอ, ภาษาอื่นโผล่เมื่อมีคลิป) เกณฑ์เท่าเดิม (10 คลิป/300 views/5 จุด) แค่แต่ละช่องนับของตัวเอง, `by_category` กรองด้วยไม่งั้น `เทค` กับ `tech` อยู่ตารางเดียวกัน. **RESULT_TOPIC** เพิ่ม pattern อังกฤษ (beat, defeated, final score, standings, who won, last night ฯลฯ) ใน regex เดียวกับไทย `!` นำหน้ายังข้ามด่านได้เหมือนเดิม. การ์ดตามช่องมีแล้ว — youtube.py อ่านทุกค่าตาม env prefix ของ Locale (YOUTUBE_ / YOUTUBE_EN_) ไม่มี fallback ร่วม, deliver() เรียก configured(locale) ไม่มี credential อังกฤษ = ปุ่มไม่โผล่, do_upload() ใช้ last_locale ที่ snapshot ตอน deliver, add_captions ใส่ language ตาม Locale (อังกฤษได้ en), history.record เก็บ locale ต่อ entry (ของเก่าไม่มี = ไทย), CATEGORY_ID/PRIVACY แยกตาม Locale เช่นกัน (default 28/public). **คู่ 2 ภาษา** (`/both <หัวข้อ>` หรือปุ่มแถวล่าง 🌏 ในลิสต์ `/trends`): ทำไทยก่อนจนคลิปส่งเข้า Telegram แล้ว `do_render` ค่อย spawn `continue_pair()` เขียนอังกฤษต่อ — **ทีละภาษา ไม่พร้อมกัน** เพราะสองสคริปต์บนจอพร้อมกันบนมือถืออ่านไม่ไหว และสั่งแก้ต้องระบุทุกครั้งว่าอันไหน. อังกฤษ**เขียนใหม่ ไม่ใช่แปล** (บรรทัด 24 vs 34 ตัว hook คนละแบบ) แต่ส่งสคริปต์ไทยที่อนุมัติแล้วเข้า prompt เป็น context (`script._sibling_note()`). manifest ทั้งคู่เก็บ `pair_id` = clip_id ฝั่งไทย. render พังหรือกด 🗑 = ยกเลิกฝั่งอังกฤษด้วย (pop `state["pair"]` พร้อมบอกในแชท), variant สุ่มแยกอิสระสองฝั่ง (ล็อกให้เหมือนกัน = แต่ละช่องเก็บ arm ช้าลงครึ่งหนึ่ง). ปุ่มเลขเดิม + รอบอัตโนมัติยังไทยอย่างเดียว (กระแสไทยส่วนใหญ่คนอเมริกาไม่แคร์ + รอบอัตโนมัติไม่มีคนรีวิว). **ปุ่มอัปพก clip_id** (`upload:<clip_id>`) + `state["uploads"]` เก็บสูงสุด 10 คลิป — เดิมปุ่มไม่พก id คลิปที่ render ทีหลังทับ `last_*` แล้วปุ่มเก่าอัปคลิปผิด (บั๊กที่มีมาก่อนฟีเจอร์นี้), ปุ่มเก่าที่ไม่มี id ยังหมายถึงคลิปล่าสุดเหมือนเดิม. **ตัวเลขทุกตัวแยกช่องแล้ว**: `history` เก็บ locale ต่อ entry แล้ว `video_ids(locale)`/`recent_titles(locale)` กรอง (ไม่ใส่ = ทุกช่อง, ของเก่าไม่มีฟิลด์ = ไทย), `analytics.*(locale)` ใช้ token ของช่องนั้น **Gate ของ ADR 0004 นับแยกช่อง เลขเท่าเดิม 30/300**, `snapshots.run()` ยิง Analytics ทีละช่อง (cap 50 ids เป็นเพดานความยาว URL ต่อ request ไม่ใช่ต่อวัน) ช่องไม่มี credential = ข้ามพร้อม log, ช่องพัง = catch ไว้ไม่ให้อีกช่องเสียรอบ — id ของอีกช่องไม่ error แต่ไม่มีแถวกลับมา อ่านเหมือน "ยังไม่ประมวลผล" เลยต้องแยกยิง. `retention.fetch(..., locale)` อ่าน locale จาก manifest ของคลิป ไม่มี `/retention <lang>` ให้จับคู่ผิด. `/stats` พิมพ์ทีละช่องที่มี credential (ตอนนี้ไทยช่องเดียว = output เหมือนเดิม). dashboard มีคอลัมน์ ภาษา + `/experiment` แยก section ต่อ locale. **ยังเหลือ: ช่องที่สองจริงๆ** — ลำดับสำคัญ: consent screen `In production` → `scripts/youtube_auth.py` (ล็อกอินบัญชีเจ้าของช่องอังกฤษ) → `make edit-vault` ใส่ `stacks.shorts_factory.youtube_en.*` (**เขียนทับ path ไทยเมื่อไหร่ อัปไทยเด้งเข้าช่องอังกฤษเงียบๆ**) → **ค่อย**เพิ่ม `YOUTUBE_EN_*` ใน secrets.manifest.yaml (ใส่ก่อนมีค่าใน vault = `render_env.py` โยน `missing vault path` แล้ว `make secrets` พังทั้ง repo ไม่ใช่แค่ stack นี้) → `make secrets` + deploy. ระหว่างนี้คลิปอังกฤษยังก๊อปจาก /volume1/shorts/en ไปอัปเอง และไม่มีตัวเลขฝั่งอังกฤษเลย |

---

## 🚀 Release & Security Process

เมื่อใช้คำสั่ง `/release` **ให้ปฏิบัติเรียงตามลำดับอย่างเคร่งครัดก่อน Commit เสมอ:**

1. **Update Stack README:** แก้ไขฟีเจอร์/สถาปัตยกรรมของ Stack ไหน ให้เข้าไปอัปเดต `README.md` ของโฟลเดอร์นั้นทันที
2. **Update Root Docs:** หากมีการเปลี่ยนพอร์ต, เพิ่ม Stack ใหม่ หรือเพิ่ม Env Configuration ให้แก้ไข `CLAUDE.md` และ `README.md` ที่ root
3. **Atomic Commit:** รวมไฟล์เอกสารที่อัปเดตทั้งหมดเข้ากับ Commit ของซอร์สโค้ดหลักในรอบนั้นทันที (ห้ามแยก Commit เอกสารออกจากโค้ด)
4. **Security Guardrail:** ห้าม Commit `.env`, `.env.deploy`, `secrets/vault.yaml` (plaintext intermediate) หรือ Hardcode ไอพี/รหัสผ่านจริงลงในโค้ดหรือเอกสาร ให้แทนที่ด้วย Placeholder เช่น `<NAS_HOST>`, `<NAS_USER>`, `<NAS_SUDO_PASSWORD>` ทุกครั้ง — `secrets/vault.sops.yaml` commit ได้เพราะ encrypted แล้ว
5. **Vault Edits:** ใช้ `make edit-vault` (sops จัดการ encrypt/decrypt ในที่) ห้ามแก้ `vault.sops.yaml` ตรงๆ ใน editor